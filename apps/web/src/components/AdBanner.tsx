interface AdBannerProps {
  slot: string;
  format?: string;
  className?: string;
}

export function AdBanner(_props: AdBannerProps) {
  // Disabled until AdSense approval — remove this early return to re-enable
  return null;
}
